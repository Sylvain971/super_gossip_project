<h4>Ne rien manquer des coulisses de The Hacking Project</h4></br></br></br></br></br>

<ul class="nav justify-content-center">
<% if moussaillon_signed_in? %>
	<div class="container">
	<table class="table table-hover">
		<thead>
  			<tr>
   	 			<th>Titre</th>
    			<th>Contenu</th>
    			<th>Délateur</th>
    			<th>Show</th>
    			<th>Edit</th>
    			<th>Destroy</th>
  			</tr>
  		</thead>
  		<tbody>
				<% @gossips.each do |gossip| %>
				<tr>
				<td><%= gossip.title %></td>
				<td><%= gossip.content %></td>
				<td><%= gossip.moussaillon.anonymous_username %></td>
				<td><%= link_to 'Show', gossip_path(gossip) %></td>
     			<td><%= link_to 'Edit', edit_gossip_path(gossip) %></td>
      			<td><%= link_to 'Destroy', gossip_path(gossip),
              		method: :delete,
              		data: { confirm: 'Are you sure?' } %></td>
			</tr>
		</tbody>
		<%end%>
	</table>

	</div>

		<li class="nav-item">
			<a class="nav-link" href="#">
			<%= link_to "Edit Account", edit_moussaillon_registration_path %></a>
		</li>

		<li class="nav-item">
			<a class="nav-link" href="#">
		<%= link_to "Sign Out", destroy_moussaillon_session_path, method: :delete %></a>
		</li>

<% else %>
<li>
<%= link_to "Sign In", new_moussaillon_session_path %>
</li>
<li>
<%= link_to "Sign Up", new_moussaillon_registration_path %>
</li>
</ul>

<%end%>